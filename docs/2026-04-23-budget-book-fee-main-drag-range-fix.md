# 取费程序主工作区拖动边界修正说明

## 本次修正内容

- 修正顶部主标签 `取费程序` 工作区中两条分隔线的最大拖动范围：
  - 左右分隔线现在可以继续向左拖到工作区最左边
  - 上下分隔线现在可以继续向上拖到主标签下方边框线位置

## 本次实现调整

- 将左侧分栏宽度最小值从固定值放开到 `0`，允许左侧分类区完全收起。
- 将左侧上下分栏的上半区保底高度从较大的固定值收敛到仅保留分隔条自身高度，允许上方分类区完全收起。
- 去掉 `取费程序` 主工作区外层的统一内边距，避免分隔条在贴边前被留白挡住。
- 给左侧分栏补充 `min-width: 0` 与 `box-sizing: border-box`，避免拖到极限时仍残留边框宽度。
- 将右侧树表区域的留白单独放到右侧容器内部，保证整体布局仍然整齐。

## 影响文件

- `src/views/cost/project-management/budget-book.vue`
  - 调整 `取费程序` 主工作区左右拖动边界
  - 调整 `取费程序` 主工作区上下拖动边界
  - 调整外层留白与左侧容器盒模型

## 校验情况

- 已执行 `pnpm typecheck`
- 结果：通过
