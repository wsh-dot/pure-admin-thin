# 预算书空明细表模拟数据补充

## 修改内容

- 在 `budgetBookMock.ts` 中新增清单计价 `主材设备` 明细模拟数据模板。
- 在 `budgetBookMock.ts` 中新增措施项目 `人材机` 明细模拟数据模板。
- 在 `budgetBookMock.ts` 中新增措施项目 `主材设备` 明细模拟数据模板。
- 将分部分项清单行的 `主材设备` 空数据源替换为 3 条模拟数据。
- 将措施项目清单行、分部行的 `人材机` 和 `主材设备` 空数据源替换为 3 条模拟数据。

## 实现功能

- 清单计价分部分项页面中，切换到截图涉及的 `主材设备` 明细页签时，不再显示“暂无数据”。
- 清单计价措施项目页面中，切换到截图涉及的 `人材机` 明细页签时，不再显示“暂无数据”。
- 清单计价措施项目页面中，切换到截图涉及的 `主材设备` 明细页签时，不再显示“暂无数据”。
- 已有上方主表数据未调整。

## 对应文件

- `src/views/cost/project-management/budgetBookMock.ts`
